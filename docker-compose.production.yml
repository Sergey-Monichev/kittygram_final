

services:
  backend:
    image: semo18/kittygram_backend
    env_file: .env
    volumes:
      - static:/backend_static/
      - media:/backend_media/
    depends_on:
      - db

  frontend:
    image: semo18/kittygram_frontend 
    volumes:
      - static:/frontend_static/

  gateway:
    image: semo18/kittygram_gateway 
    ports:
      - "80:80"
    depends_on:
      - backend
      - frontend
    volumes:
      - static:/static/
      - media:/media/

  db:
    image: postgres:13
    volumes:
      - pg_data:/var/lib/postgresql/data
    env_file: .env

volumes:
  static:
  media:
  pg_data:
